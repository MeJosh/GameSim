"""Optional local web adapter for playing games against GameSim agents.

The package is intentionally separate from ``core``, ``games``, and ``rl``. Its
only job is to translate browser requests into existing engine and agent calls.
"""

from __future__ import annotations

__all__: list[str] = []
